use std::path::Path;

fn main() {
    // let args: Vec<String> = env::args().collect();
    // build_index(Path::new("/Volumes/workplace/Tantivy-JNI-Prototype/mylib/src/idx"), 2).unwrap();
    println!("hello");
}
